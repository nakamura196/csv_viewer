{
  "name": "csv_viewer",
  "private": true,
  "type": "module",
  "description": "Static CSV viewer served from docs/ by GitHub Pages at https://csv.ldas.jp",
  "scripts": {
    "test": "node --test tests/*.test.mjs && node scripts/check-site.mjs",
    "test:site": "node scripts/check-site.mjs",
    "test:live": "node scripts/check-live.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
